# Menu.X Frontend Environment Variables Example
# Copy this file to .env.local and configure your values
# DO NOT commit .env.local to version control!

# Backend API URL
VITE_API_BASE_URL=http://localhost:8080/api

# Application Configuration
VITE_APP_NAME=Menu.X
VITE_APP_VERSION=1.0.0

# Note: AI provider API keys are NOT configured here
# They are managed by Super Admin through the web interface
# and stored encrypted in the database for security
