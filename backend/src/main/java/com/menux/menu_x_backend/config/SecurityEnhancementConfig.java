package com.menux.menu_x_backend.config;

import org.springframework.context.annotation.Configuration;

/**
 * Enhanced security configuration for production-grade security
 * Note: CORS configuration is handled in SecurityConfig.java to avoid conflicts
 */
@Configuration
public class SecurityEnhancementConfig {
    // CORS configuration moved to SecurityConfig.java to prevent conflicts
    // Additional security enhancements can be added here as needed
}
